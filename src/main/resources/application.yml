logging:
  level:
    org.hibernate.SQL: "DEBUG"

spring:
  application:
    name: "todo"

  profiles:
    active: "dev"

  datasource:
    driver-class-name: "org.sqlite.JDBC"
    url: "jdbc:sqlite:db.sqlite3"

  jpa:
    hibernate:
      ddl-auto: "update"